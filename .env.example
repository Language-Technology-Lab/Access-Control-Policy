# Copy to `.env` and fill in real values. Never commit `.env`.
# Placeholders only — replace with your own secrets.

OPENAI_API_KEY="<OPENAI_API_KEY>"

# Optional: cap longest image side when resizing for the API (default in code: 2048)
# MAX_IMAGE_SIDE="2048"
